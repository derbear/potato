POTATO is an interpreter for a LISP/Scheme-like language written in C.

`make' to build
`make clean' to cleanup

`./potato' to run
`./potato [filename]' to execute a file

TODO
 * assertions
 * debugger
 * garbage-collection
 * compiler
 * floating-point
